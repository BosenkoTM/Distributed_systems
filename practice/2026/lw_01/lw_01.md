# Лабораторная работа №01
## Тема: Реализация RPC-сервиса с использованием gRPC

### 🎯 Цель работы
*   Освоить принципы удаленного вызова процедур (RPC) и их применение в распределенных системах.
*   Изучить основы фреймворка gRPC и языка определения интерфейсов Protocol Buffers (Protobuf).
*   Научиться определять сервисы и сообщения с помощью Protobuf.
*   Реализовать клиент-серверное приложение на языке Python с использованием gRPC.
*   Получить практические навыки в генерации кода, реализации серверной логики и клиентских вызовов для различных типов RPC.

### 🛠 Инструменты и технологический стек
*   **Операционная система:** Ubuntu 20.04.6 LTS ([Ссылка на образ](https://disk.yandex.ru/d/q-gamKx9XBseKw)).
*   **Язык программирования:** Python 3.8+.
*   **Среда управления зависимостями:**
    *   `venv` – стандартный модуль Python для создания изолированных виртуальных окружений.
*   **Ключевые библиотеки:**
    *   `grpcio` – основная библиотека с реализацией gRPC для Python (runtime).
    *   `grpcio-tools` – набор инструментов, включающий компилятор `protoc` для генерации Python-кода из `.proto` файлов.

---

### 📚 Теоретическая часть

#### Основные понятия
**RPC (Remote Procedure Call)** — технология, позволяющая программе вызывать процедуру (функцию, метод) в другом адресном пространстве (на удаленном узле), как если бы это был локальный вызов. Скрывает сложность сетевого взаимодействия.

**gRPC** — высокопроизводительный фреймворк RPC с открытым исходным кодом от Google.
*   *Особенности:* использует **Protocol Buffers** для сериализации, работает поверх **HTTP/2** (мультиплексирование, стриминг), поддерживает множество языков (Python, Go, Java, C++ и др.).

**Protocol Buffers (Protobuf)** — механизм сериализации структурированных данных. Структура данных описывается в `.proto` файле, на основе которого компилятор генерирует код для работы с данными на целевом языке программирования.

#### Типы RPC-вызовов в gRPC
1.  **Unary RPC:** Простой синхронный вызов. Клиент отправляет один запрос — получает один ответ.
2.  **Server streaming RPC:** Клиент отправляет один запрос — получает поток сообщений от сервера.
3.  **Client streaming RPC:** Клиент отправляет поток сообщений — сервер отвечает одним сообщением после получения всего потока.
4.  **Bidirectional streaming RPC:** Двунаправленный обмен потоками данных. Клиент и сервер могут отправлять сообщения независимо и в любом порядке.

---

### 💻 Порядок выполнения работы

1.  **Получение задания:** Узнать свой номер варианта у преподавателя.
2.  **Настройка окружения:**
    *   Установить Python и `pip`.
    *   Создать виртуальное окружение и установить библиотеки: `pip install grpcio grpcio-tools`.
3.  **Создание контракта:**
    *   Создать файл с расширением `.proto`.
    *   Описать сервис, методы и структуры сообщений согласно варианту.
4.  **Генерация кода:**
    *   Скомпилировать `.proto` файл с помощью утилиты `protoc`, получив Python-файлы (стабы и сообщения).
5.  **Реализация сервера:**
    *   Создать класс, наследующий сгенерированный базовый класс сервиса (`Servicer`).
    *   Реализовать бизнес-логику методов.
    *   Запустить gRPC сервер.
6.  **Реализация клиента:**
    *   Создать канал подключения (`Channel`).
    *   Создать стаб (`Stub`) и выполнить вызовы методов сервера.
7.  **Тестирование:** Запустить сервер и клиент, проверить корректность обмена данными.
8.  **Отчет:** Зафиксировать результаты и оформить документацию.

---

### 📝 Варианты заданий

Выберите вариант задания, соответствующий вашему номеру в списке группы.

[🔗 Перейти к списку вариантов заданий на образовательном портале](https://envlab.ru/mod/assign/view.php?id=1017)

---

### 📄 Требования к отчету

Отчет предоставляется в электронном виде и должен содержать:
1.  Титульный лист.
2.  Цель работы.
3.  Номер и описание выполняемого варианта.
4.  **Листинг .proto файла** с комментариями, поясняющими структуру данных.
5.  **Листинг server.py** с комментариями к реализации методов.
6.  **Листинг client.py** с комментариями к вызовам.
7.  **Скриншоты работы**, демонстрирующие вывод консоли сервера и клиента (успешные запросы/ответы).
8.  Краткие выводы по проделанной работе.

### 💯 Критерии оценки (макс. 10 баллов)

| Баллы | Критерии |
|:---:|---|
| **1-2** | Корректно составлен `.proto` файл, описаны сообщения и сервисы в соответствии с заданием. |
| **3-5** | Реализована серверная часть (`server.py`). Сервер запускается, логика методов соответствует заданию. |
| **6-8** | Реализована клиентская часть (`client.py`). Клиент корректно подключается, вызывает методы и обрабатывает ответы. |
| **9-10** | Работа выполнена полностью. Код структурирован, содержит комментарии. Отчет оформлен по требованиям, присутствуют осмысленные выводы и скриншоты. |