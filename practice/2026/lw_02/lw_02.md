# Лабораторная работа №02. Проектирование и реализация клиент-серверной системы. HTTP, веб-серверы и RESTful веб-сервисы

### 🎯 Цель работы
*   Изучить методы отправки и анализа HTTP-запросов с использованием инструментов `telnet` и `curl`.
*   Освоить базовую настройку и анализ работы HTTP-сервера `nginx` в качестве веб-сервера и обратного прокси.
*   Изучить и применить на практике концепции архитектурного стиля REST для создания веб-сервисов (API) на языке Python.

### 🛠 Инструменты и технологический стек
*   **Операционная система:** Ubuntu 20.04.6 LTS (в рамках предоставленного образа).
*   **Сетевые утилиты:** `telnet`, `curl`.
*   **Веб-сервер:** `nginx`.
*   **Среда разработки:**
    *   **Интерпретатор:** Python 3.8+.
    *   **Управление пакетами:** `python3-pip`.
    *   **Виртуальное окружение:** `python3-venv`.
    *   **Фреймворк:** Flask (для реализации REST API).
*   Доступ к сети Интернет.

---

### 📚 Теоретическая часть

**1. HTTP (HyperText Transfer Protocol)**
Протокол прикладного уровня для передачи данных.
*   **Запрос.** Метод (GET, POST и др.), URI (адрес), заголовки, тело (опционально).
*   **Ответ.** Код состояния (200 OK, 404 Not Found и др.), заголовки, тело ответа.

**2. Инструменты анализа**
*   **Telnet.** Позволяет отправлять "сырые" байты HTTP-запросов вручную, полезен для понимания структуры протокола.
*   **Curl.** Мощный инструмент для автоматизированной отправки запросов. Поддерживает HTTPS, редиректы, управление заголовками.

**3. Nginx**
Высокопроизводительный веб-сервер, выполняющий роли:
*   **Веб-сервер.** Раздача статики (HTML, CSS).
*   **Обратный прокси (Reverse Proxy).** Перенаправление запросов от клиентов к внутренним приложениям (например, Flask). Позволяет скрыть архитектуру, кэшировать запросы и балансировать нагрузку.

**4. REST и RESTful API**
**REST (Representational State Transfer)** — архитектурный стиль распределенных систем.
*   **Stateless:** Сервер не хранит состояние клиента между запросами.
*   **Стандартные методы HTTP:**
    *   `GET` — получить данные.
    *   `POST` — создать ресурс.
    *   `PUT`/`PATCH` — обновить ресурс.
    *   `DELETE` — удалить ресурс.

---

### 💻 Порядок выполнения работы

#### Шаг 1. Анализ HTTP-запросов
1.  **Установка утилит:**
    ```bash
    sudo apt update && sudo apt install telnet curl -y
    ```
2.  **Ручной запрос через Telnet:**
    Подключитесь к `mgpu.ru` и отправьте запрос вручную:
    ```bash
    telnet mgpu.ru 80
    ```
    *После подключения введите:*
    ```http
    GET / HTTP/1.1
    Host: mgpu.ru
    
    ```
    *(Нажмите Enter дважды!)*
    *Анализ:* Сервер вернет код **301 Moved Permanently**, так как требуется HTTPS.
3.  **Автоматический запрос через Curl:**
    ```bash
    curl -v http://mgpu.ru
    ```
    *Анализ:* Curl покажет редирект (301) и автоматически выполнит запрос по новому адресу (200 OK).

#### Шаг 2. Настройка Nginx
1.  **Установка и запуск:**
    ```bash
    sudo apt install nginx -y
    sudo systemctl start nginx
    sudo systemctl enable nginx
    ```
2.  **Проверка:** Откройте в браузере `http://localhost`. Должна отобразиться приветственная страница.

#### Шаг 3. Реализация REST API на Flask
1.  **Подготовка окружения:**
    ```bash
    mkdir ~/rest_api_lab && cd ~/rest_api_lab
    python3 -m venv venv
    source venv/bin/activate
    pip install Flask
    ```
2.  **Код приложения (`app.py`):**
    Создайте файл с базовым API управления задачами (см. пример в методичке).
    *   Реализуйте методы `GET /api/tasks`, `GET /api/tasks/<id>`, `POST /api/tasks`.
3.  **Запуск:**
    ```bash
    python3 app.py
    ```
    *Сервер запустится на порту 5000.*

#### Шаг 4. Настройка Nginx как обратного прокси
1.  **Редактирование конфига:**
    ```bash
    sudo nano /etc/nginx/sites-available/default
    ```
2.  **Добавление проксирования:**
    В блок `server` добавьте:
    ```nginx
    location /api/ {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    ```
3.  **Перезапуск:**
    ```bash
    sudo nginx -t
    sudo systemctl restart nginx
    ```

#### Шаг 5. Тестирование системы
Используйте `curl` для проверки работы API через порт 80 (Nginx):
1.  **Получить задачи:** `curl http://localhost/api/tasks`
2.  **Создать задачу:**
    ```bash
    curl -X POST -H "Content-Type: application/json" \
    -d '{"title": "Test via Nginx"}' http://localhost/api/tasks
    ```

---

### 📝 Варианты заданий

Каждый вариант включает три части:
1.  **HTTP-анализ:** Исследование ресурса.
2.  **REST API:** Разработка сервиса на заданную тему.
3.  **Настройка Nginx:** Реализация специфического сценария.

[🔗 Перейти к списку вариантов заданий на образовательном портале](https://envlab.ru/mod/assign/view.php?id=1018)

---

### 📄 Требования к отчету

1.  Титульный лист.
2.  Цель работы.
3.  Краткие теоретические сведения (HTTP, REST, Nginx).
4.  Номер и описание варианта задания.
5.  **Ход выполнения:**
    *   Часть 1. Скриншоты/текст команд `telnet` и `curl` с анализом ответов (коды состояния, заголовки).
    *   Часть 2. Листинг кода `app.py` с комментариями.
    *   Часть 3. Листинг конфигурации Nginx (измененные части).
    *   Часть 4. Результаты тестирования (скриншоты вызова `curl` для всех эндпоинтов).
6.  Выводы.

### 💯 Критерии оценки (макс. 10 баллов)
*   **0-3 балла.** Базовая настройка Nginx, попытка реализации API.
*   **4-6 баллов.** Работающий API, корректная настройка проксирования.
*   **7-8 баллов.** Выполнено индивидуальное задание, корректный анализ HTTP-запросов.
*   **9-10 баллов.** Полное выполнение всех пунктов, детальный отчет, глубокий анализ заголовков и кодов ответа.